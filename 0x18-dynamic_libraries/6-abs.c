#include "main.h"
/**
* _abs - prints absolute value
* @x: first argument
* Description: prints absolute value of a given number;
* Return: always 0
*/
int _abs(int x)
{
	if (x < 0)
	{
		return (-x);
	}
		return (x);
}
