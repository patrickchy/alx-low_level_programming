#include <stdio.h>

/**
* _isdigit - checks for digits
* @c: operand
* Return: always 0 (successful)
*/
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
		return (0);
	return (1);
}
