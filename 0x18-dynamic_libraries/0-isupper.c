#include <stdio.h>
#include "main.h"
/**
* _isupper - distinguises upper and lower cased letters
* @letter: test letter
* Return: always 0 (successful)
*/

int _isupper(int letter)
{
	if (letter >= 'A' && letter <= 'Z')
	{
		return (1);
	}
		return (0);
	return (0);
}
